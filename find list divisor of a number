
def findDivisor(number):
    divisor = []
    for x in range(1,number+1):
        if number%x==0:
            divisor.append(x)
    return divisor

number = int(input('enter number'))
listOfDivisor = findDivisor(number)
print(listOfDivisor)
